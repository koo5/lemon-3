
int min(int a, int b) {
	return a < b ? a : b;
}
int max(int a, int b) {
	return a > b ? a : b;
}

int in(int a, int b, int c) {
	return (a <= b && c >= b);
}

float floabs(float x) {
	return x > 0 ? x : -x;
}
